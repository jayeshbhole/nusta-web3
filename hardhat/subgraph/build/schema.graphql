# type ExampleEntity @entity {
#     id: ID!
#     count: BigInt!
#     owner: Bytes! # address
#     spender: Bytes! # address
# }

type Post
    @entity
    @key(fields: "id")
    @key(fields: "author")
    @key(fields: "ipfsHash") {
    id: ID!
    author: Author! # address
    ipfsHash: String!
}

type Author @entity {
    id: ID!
    posts: [Post!]!
}
